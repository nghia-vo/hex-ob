# The two packages are independent nested git repos (own history, own
# remotes) — hex-ob tracks only sandbox-level docs/glue. See README.md.
/hextools/
/hex-profile-collection/

AGENTS.md
REQUIRES.md
ref


# local env/tooling
.pixi/
.venv/
__pycache__/
.idea
# hex-simulated-beamline: machine-local state, never committed
hex-simulated-beamline/.toolenv/
hex-simulated-beamline/.reflow2/
hex-simulated-beamline/**/__pycache__/

# generated TLS material for the sim's Redis (scripts/up.sh recreates on demand)
hex-simulated-beamline/compose/certs/*.crt
hex-simulated-beamline/compose/certs/*.key
